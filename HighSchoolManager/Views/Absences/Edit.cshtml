@model HighSchoolManager.Data.Absence

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Absence</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Absence_id" />
            <div class="form-group">
                <label asp-for="Etudiant_FK" class="control-label"></label>
                <select asp-for="Etudiant_FK" class="form-control" asp-items="ViewBag.Etudiant_FK"></select>
                <span asp-validation-for="Etudiant_FK" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Seance_FK" class="control-label"></label>
                <select asp-for="Seance_FK" class="form-control" asp-items="ViewBag.Seance_FK"></select>
                <span asp-validation-for="Seance_FK" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
