@model IEnumerable<HighSchoolManager.Data.TypeEquippement>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Type_Equippement_designation)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type_Equippement_designation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Type_Equippement_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Type_Equippement_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Type_Equippement_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
